# Example YAML payloads
#
# This file is intentionally simple and generic. For HDMI Input simulation scenario templates,
# see:
#   ./hdmiinput-scenarios/*.yaml
#
# You can set `file_name` in python-client-send-yaml.py to one of those scenario files and send it.

---
test1:
  yamlData: somevalue
  x: 1
  on: true
test2:
  yamlData1: somevalue1
  y: 2
  off: false
